# Real NestJS backend base URL. When unset, the in-app dev mock is used.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Force the in-app dev mock even when a base URL is set.
NEXT_PUBLIC_USE_MOCK=1
